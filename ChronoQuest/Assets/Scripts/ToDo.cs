using UnityEngine;

public class ToDo : MonoBehaviour
{
    /*
     * Basic Player Movement - Working On
     * Fix: Player Spawns in ground - DONE
     * Fix: Player tips over - DONE
     * Fix: Player moves too fast
     * Camera follows Player POV
     */

    /*
     * Other Checklists
     */
}
